{
  "name": "@pounce/transcript",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Normalize coding-agent (Claude Code, Codex, opencode, …) session-transcript message bodies: strip agent-specific plumbing tags, surface slash-commands and captured output. Framework-agnostic, zero runtime dependencies.",
  "license": "MIT",
  "main": "./src/index.js",
  "types": "./src/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.d.ts",
      "default": "./src/index.js"
    }
  },
  "files": [
    "src"
  ],
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vitest": "^2.1.0"
  }
}
