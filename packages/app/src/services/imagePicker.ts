/**
 * Image attachment seam. Mobile: expo-image-picker (photo library). Desktop
 * overrides this per-platform (imagePicker.desktop.ts) until a native file
 * picker is wired up there.
 */
export * from "expo-image-picker";
